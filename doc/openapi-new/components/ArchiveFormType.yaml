nelmio_api_doc:
  documentation:
    components:
      schemas:
        ArchiveFormType:
          required:
          - beforeDate
          properties:
            beforeDate:
              type: string
              format: date
          type: object

