nelmio_api_doc:
  documentation:
    components:
      schemas:
        BadgeType:
          required:
          - badgeTypeId
          - name
          - createdAt
          - updatedAt
          properties:
            badgeTypeId:
              type: string
              format: uuid
            name:
              type: string
            createdAt:
              type: string
              format: date-time
            updatedAt:
              type: string
              format: date-time
          type: object
          additionalProperties: false

