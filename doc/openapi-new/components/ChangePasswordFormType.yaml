nelmio_api_doc:
  documentation:
    components:
      schemas:
        ChangePasswordFormType:
          required:
          - currentPassword
          - plainPassword
          properties:
            currentPassword:
              type: string
              format: password
            plainPassword:
              type: string
              format: password
          type: object

