nelmio_api_doc:
  documentation:
    components:
      schemas:
        CustomEventSchema:
          properties:
            eventType:
              type: string
            name:
              type: string
            schema:
              $ref: '#/components/schemas/Schema'
            active:
              type: boolean
            createdAt:
              type: string
              format: date-time
          type: object

