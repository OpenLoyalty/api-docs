nelmio_api_doc:
  documentation:
    components:
      schemas:
        CustomerWasDeactivated:
          required:
          - customerId
          - type
          - createdAt
          properties:
            customerId:
              type: string
            type:
              type: string
              enum:
              - CustomerWasDeactivated
            accountId:
              type: string
            createdAt:
              type: string
              format: date-time
            flatVariables:
              type: object
          type: object
          additionalProperties: false

