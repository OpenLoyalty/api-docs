nelmio_api_doc:
  documentation:
    components:
      schemas:
        InvalidToken:
          required:
          - code
          - message
          properties:
            code:
              type: string
              example: 401
            message:
              type: string
              example: Invalid JWT Token
          type: object

