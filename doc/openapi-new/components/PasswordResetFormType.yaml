nelmio_api_doc:
  documentation:
    components:
      schemas:
        PasswordResetFormType:
          required:
          - plainPassword
          properties:
            plainPassword:
              type: string
              format: password
          type: object

