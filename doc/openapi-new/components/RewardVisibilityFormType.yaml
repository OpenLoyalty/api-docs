nelmio_api_doc:
  documentation:
    components:
      schemas:
        RewardVisibilityFormType:
          properties:
            allTime:
              type: boolean
            from:
              type: string
              format: date-time
            to:
              type: string
              format: date-time
          type: object

