nelmio_api_doc:
  documentation:
    components:
      schemas:
        ScrollResponse:
          required:
          - next
          properties:
            next:
              description: Scroll identifier for pagination
              type: string
              nullable: true
          type: object
          additionalProperties: false

