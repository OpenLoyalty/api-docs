nelmio_api_doc:
  documentation:
    components:
      schemas:
        SetPasswordFormType:
          required:
          - plainPassword
          properties:
            plainPassword:
              type: string
              format: password
          type: object

