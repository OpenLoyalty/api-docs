nelmio_api_doc:
  documentation:
    components:
      schemas:
        TotalLogsArchived:
          properties:
            totalArchived:
              type: integer
              example: 32
            filename:
              type: string
              example: filename
          type: object

